{
  "name": "testcaptive",
  "version": "1.0.0",
  "description": "Professional UI Behavior Recording Tool - Captures user interactions and generates test automation scripts",
  "main": "index.js",
  "scripts": {
    "build:chrome": "cd chrome-extension && npm run build",
    "build:vscode": "cd vscode-extension && npm run compile",
    "build:all": "npm run build:chrome && npm run build:vscode",
    "package:chrome": "cd chrome-extension && npm run package",
    "package:vscode": "cd vscode-extension && npm run package",
    "package:all": "npm run package:chrome && npm run package:vscode",
    "install:all": "cd chrome-extension && npm install && cd ../vscode-extension && npm install && cd ..",
    "clean:chrome": "cd chrome-extension && npm run clean",
    "clean:vscode": "cd vscode-extension && npm run clean",
    "clean:all": "npm run clean:chrome && npm run clean:vscode"
  },
  "keywords": [
    "test-automation",
    "ui-testing",
    "selenium",
    "playwright",
    "cypress",
    "chrome-extension",
    "vscode-extension",
    "recording",
    "behavior-capture"
  ],
  "author": "TestCaptive Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/testcaptive.git"
  },
  "bugs": {
    "url": "https://github.com/your-org/testcaptive/issues"
  },
  "homepage": "https://github.com/your-org/testcaptive#readme"
}
